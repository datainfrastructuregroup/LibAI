<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{ title or 'Obsidian 11ty' }}</title>
    <link rel="stylesheet" href="{{ "/" | url }}css/style.css">
    <script type="module" src="{{ "/" | url }}js/graph-visualization.js"></script>
</head>
<body>
    <div class="container">
        <header>
            <div class="header-title">
                <h1><a href="{{ "/" | url }}" class="logo">Liberation + AI Think Tank</a></h1>
                <p class="subtitle">we are asking questions</p>
            </div>
            <nav>
                <a href="{{ "/" | url }}">Home</a>
                <a href="{{ "/notes" | url }}">All Entries</a>
                <a href="{{ "/contributors" | url }}">Contributors</a>
                <a href="{{ "/about" | url }}">About</a>
                <a href="{{ "/bibliography" | url }}">Bibliography</a>
            </nav>
        </header>
        
        <main>
            {{ content | safe }}
        </main>
        
        <section class="graph-section">
            <h2>Knowledge Graph</h2>
            <div id="force-graph" class="force-graph-container"></div>
        </section>
        
        <footer>
            <p>Built with <3 by students at MIT and Smith College.</p>
        </footer>
    </div>
    <script type="module" src="{{ "/" | url }}js/main.js"></script>
</body>
</html>
